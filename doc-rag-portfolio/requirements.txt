# Document RAG + long-form generation — portfolio project
# Python 3.10+

# PDF
pypdf>=3.0.0

# Embeddings (free, local — no API key needed for indexing)
sentence-transformers>=2.2.0

# Vector store (local, no cloud signup required)
chromadb==0.4.24
pysqlite3-binary>=0.5.0

# LLM for chat + report generation (needs API key)
openai>=1.12.0

# App
streamlit>=1.29.0

# Utilities
python-dotenv>=1.0.0
tiktoken>=0.5.0
