# Used by Streamlit Cloud — same deps as doc-rag-portfolio
# Repo root so Cloud finds it when main file is doc-rag-portfolio/app/main.py

pypdf>=3.0.0
sentence-transformers>=2.2.0
# Chroma: 0.4.x breaks on Python 3.13 + NumPy 2 (np.float_ removed). Use 0.5.23+.
chromadb==0.5.23
pysqlite3-binary>=0.5.0
openai>=1.12.0
streamlit>=1.29.0
python-dotenv>=1.0.0
tiktoken>=0.5.0
